{
  "name": "repo-org",
  "private": true,
  "packageManager": "pnpm@9.15.0",
  "scripts": {
    "build": "pnpm -r run build",
    "dev": "pnpm --filter @repo-org/api dev",
    "db:generate": "pnpm --filter @repo-org/api db:generate",
    "db:migrate": "pnpm --filter @repo-org/api db:migrate",
    "start": "pnpm --filter @repo-org/api start"
  }
}
